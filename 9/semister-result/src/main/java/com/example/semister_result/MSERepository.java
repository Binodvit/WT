package com.example.semister_result;

import com.example.semister_result.MSE;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MSERepository extends JpaRepository<MSE, String> {
}
